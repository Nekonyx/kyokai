import { createTheme } from '@nextui-org/react'

export const theme = createTheme({
  type: 'light',
  theme: {
    fonts: {
      sans: 'Nunito, "Segoe UI", -apple-system, BlinkMacSystemFont, sans-serif',
      mono: 'Menlo, Monaco, "Lucida Console"'
    }
  }
})
